{
  "name": "citadel-hotel",
  "private": true,
  "version": "1.0.0",
  "description": "Citadel Hotel PMS - Full Stack (Monorepo)",
  "author": "Taha Bikki",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm install && npm run build",
    "start": "cd app && npm run start",
    "lint": "cd app && npm run lint"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.3",
    "@supabase/supabase-js": "^2.105.4"
  }
}
